-- local draw_surface = require('basic_drawable').draw_surface

-- draw_surface(800, 600)
--

general_options = {
	sloppyfocus = false,
}

awful.spawn("pamixer --set-volume 50")

some.hello_world()
some.quit()
